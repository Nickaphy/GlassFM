# Keybindings

## Navigation
`h` - back dir 
`l` - forward dir
`k` - up
`j` - down

Leader (SPC):
`cd` - jump to arbitrary path (type it)
`mk` - create file/dir (prompts which)
`mv` - move selected
`cp` - copy selected
`rn` - rename selected
`rm` - delete selected (with confirm)